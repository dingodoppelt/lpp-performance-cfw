#Build on Linux
```
make
amidi -l
amidi -p <see above for port> -s build/cfw.sys
```

##Use Color Palette Tool
- run tools/createColorPalette.py
- use [the Launchpad Utility](https://fw.mat1jaczyyy.com/firmware) to flash the created Palette in tools
